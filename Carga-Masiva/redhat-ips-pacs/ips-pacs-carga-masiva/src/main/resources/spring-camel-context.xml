<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:camel="http://camel.apache.org/schema/spring"
	xsi:schemaLocation="
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
         http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd">

	<!-- Referencia a definiciones bean de spring DSL -->
	<import resource="bean-definitions.xml"/>
	
	<!-- Referencia a rutas principales del flujo -->
	<import resource="routes/routeExceptions.xml"/>
	<import resource="routes/routeBussiness.xml"/>

	<!-- Contexto general del componente camel -->
	<camelContext id="camelContextBase"	xmlns="http://camel.apache.org/schema/spring" trace="false">
        
        <propertyPlaceholder id="placeholder" location="classpath:application-${PFX_ENV}.properties" />

		<!-- Referencia a rutas principales -->
		<routeContextRef ref="routeException"/>
		<routeContextRef ref="routeBussiness"/>
	
		<!-- Configuaraciones entradas json -->
	    <dataFormats>
	        <json id="request" library="Jackson" unmarshalTypeName="cl.losheroes.middleware.vo.RequestService"/>
	    </dataFormats>	

		
		<onException>
			<exception>cl.ips.pacs.exception.BadRequestException</exception>
			<handled><simple>true</simple></handled>
			<to uri="direct:badRequestHandler"/>
		</onException>
		
		<onException>
			<exception>cl.ips.pacs.exception.RuntimeServiceException</exception>
			<handled><simple>true</simple></handled>
			<to uri="direct:runtimeExceptionHandler"/>
		</onException>		

		<onException>
			<exception>java.lang.Exception</exception>
			<handled><simple>true</simple></handled>
			<to uri="direct:runtimeExceptionHandler"/>
		</onException>
		
		<!-- Configuración base REST DSL -->
		<restConfiguration bindingMode="off" enableCORS="true" component="undertow" port="8080" />
		<rest path="{{api.rest.version}}">
			<get uri="{{api.rest.operacionResumen}}" >
				<to uri="direct:runServiceCargasEmpresa"/>
			</get>			
		</rest>
		
	</camelContext>

</beans>
